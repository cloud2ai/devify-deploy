# Simplified relay configuration for development
# Allow all connections without restrictions

[main]
# Disable ACL checks completely for development
acl=false

# Allow relaying for all connections
relay_all=true

# No authentication required for relaying
auth_relay=false

# Allow relaying from any IP address
relay_hosts=*

# Allow relaying to any domain
relay_dest_domains=*
